
import { Client } from './Api';
import { JobImpl } from './impl/JobImpl'
import { User } from "./User";

export class Job {
  constructor(impl: JobImpl, app: Client)

  get user() : User

  get id(): String

  get done(): Boolean

  get state(): String

  get fileId(): String

  // Status for specified job
  refresh(): Promise<Object>

  // Update some parameters for specific job (available only for admins)
  editJob(data): Promise<Object>

  delete(): Promise<Object>
}